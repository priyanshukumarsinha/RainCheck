"use client";

export default function ContactHelperTools({ vendor }: { vendor?: any }) {
  return (
    <div>
      <h4 className="font-semibold">Contact Tools</h4>
      <p className="text-gray-400 text-sm">Generate an estimate request message or share plan with vendor.</p>
    </div>
  );
}
