"use client";

export default function ApplyNowLinks({ links }: { links?: any[] }) {
  return (
    <div className="mt-3">
      <button className="px-3 py-2 rounded bg-emerald-400 text-black font-medium">Apply for Subsidy</button>
    </div>
  );
}
