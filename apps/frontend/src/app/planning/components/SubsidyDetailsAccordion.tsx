"use client";

export default function SubsidyDetailsAccordion({ details }: { details?: any[] }) {
  return (
    <div>
      <h4 className="font-semibold text-amber-300">Available Schemes</h4>
      <p className="text-gray-400 text-sm">(Mock accordion — populate from government sources)</p>
    </div>
  );
}
